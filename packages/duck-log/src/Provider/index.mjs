import { createRequire } from 'node:module';

const require = createRequire(import.meta.url);
const Provider = require('./Provider/Provider.cjs');

export const register = (options) => {

};

export const create = (type, options) => {

}